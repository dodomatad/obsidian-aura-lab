import { describe, it, expect } from "vitest";

describe("example", () => {
  it("should pass", () => {
    expect(true).toBe(true);
  });

  it("should do basic math", () => {
    expect(2 + 2).toBe(4);
  });
});
